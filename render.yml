services:
  - type: web
    name: plc-verona-academy-backend
    env: docker
    region: frankfurt
    plan: free
    dockerfilePath: ./Dockerfile
    disk:
      name: pb-data
      mountPath: /pb/pb_data
      sizeGB: 1
    envVars:
      - key: PORT
        value: 8080
